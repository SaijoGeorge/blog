import styled from "styled-components"
import { rem, font } from "../styles"

const PunAuthor = styled.div`
  text-align: center;
  font-size: ${rem(font.small)};
  ${font.secondary};
  color: rgba(255, 255, 255, .7);
`

export default PunAuthor
